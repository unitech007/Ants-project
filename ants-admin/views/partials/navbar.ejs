</head>

<body>
	<div class="wrapper">
		<nav class="navbar navbar-expand-lg navbar-light">
			<div class="container">
				<a href="/" class="navbar-brand"><img src="/assets/images/ants-icon.png" alt="Ants icon"
						class="ants-icon"><%= __('common.ANTS') %>
				</a>
				<!-- handyman -->
				<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarMenu">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarMenu">
					<ul class="navbar-nav">
						<li class="nav-item"><a href="/services" class="nav-link"><%= __('common.services') %></a></li>
					</ul>
					<% if(currentUser){ %>
					<ul class="navbar-nav">
						<li class="nav-item"><a href="/vendor/vendor_approve_request" class="nav-link"><%= __('common.vendor_request') %></a></li>
					</ul>
					<ul class="navbar-nav">
						<li class="nav-item"><a href="/customer/list" class="nav-link"><%= __('common.customers') %></a></li>
					</ul>
					<ul class="navbar-nav">
						<li class="nav-item"><a href="/enquiry" class="nav-link"><%= __('common.enquiry') %></a></li>
					</ul>
					<ul class="navbar-nav">
						<li class="nav-item"><a href="/bookingdetails" class="nav-link"><%= __('common.Booking_details') %></a></li>
					</ul>

					<% } %>
					<ul class="navbar-nav ml-auto">
						<!-- Language Dropdown -->
						<% if (!currentUser) { %>
							<li class="nav-item dropdown">
								<a class="nav-link dropdown-toggle" href="#" id="languageDropdown" role="button"
									data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
									<%= __('common.language') %>
								</a>
								<div class="dropdown-menu" aria-labelledby="languageDropdown">
									<a class="dropdown-item" href="/change-language/en">English</a>
									<a class="dropdown-item" href="/change-language/ta">தமிழ்</a>
									<a class="dropdown-item" href="/change-language/ar">عربى</a>
								</div>
							</li>
							<% } %>
	
							<!-- User Dropdown -->
						<% if(!currentUser){ %>
							<li class="nav-item dropdown">
								<a class="nav-link dropdown-toggle" href="#" id="adminDropdownMenu" role="button"
								data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
								<%= __('common.admin') %>
								</a>
								<div class="dropdown-menu" aria-labelledby="adminDropdownMenu">
								<a class="dropdown-item" href="/admin/register"><%= __('common.admin_registration') %></a>
								<a class="dropdown-item" href="/admin/login"><%= __('common.admin_login') %></a>
								</div>
								</li>
						<% } else {%>
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" href="#" id="currentUserMenu" role="button"
								data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
								<%= __('common.signed_in_as') %> <strong><%= currentUser.username %></strong>
							</a>
							<div class="dropdown-menu" aria-labelledby="currentUserMenu">
								<a class="dropdown-item" href="/admin/<%= currentUser._id %>/edit"><i
										class="fas fa-edit"></i><%= __('common.ADMIN_EDIT_PROFILE') %></a>
								<a class="dropdown-item" href="/logout"><i class="fas fa-sign-out-alt"></i><%= __('common.LOGOUT') %></a>
							</div>
						</li>
						<% } %>
					</ul>
				</div>
			</div>
		</nav>

		<div class="container">
			<% if(error && error.length > 0){ %>
			<div style="margin-top: 16px; font-weight: 500" class="alert alert-danger alert-dismissible" role="alert">
				<a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
				<%= error %>
			</div>
			<% } %>
			<% if(success && success.length > 0){ %>
			<div style="margin-top: 16px; font-weight: 500" class="alert alert-success alert-dismissible" role="alert">
				<a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
				<%= success %>
			</div>
			<% } %>
		</div>